export class Cart {
    constructor(
        public id: number,
        public nombre: string,
     /*    public img: string, */
        public cant: number,
        public precio: number
    ) { }
}
